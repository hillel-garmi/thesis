\documentclass[../main.tex]{subfiles}

\begin{document}
  \section{Ramification at ab}
  Let $\F$ be a local field and $v$ an extension of its valuation to its algebraic closure $\overline{\F}$.
  Let 
  \[
    f = a_n x^n + a_m x^m + a_k x^k \in \F\left[ x \right]
  \]
  be a trinomial with $a_n, a_m, a_k \neq 0$, with $\gcd\left( n,m,k \right) = 1$ and with $n,m,k$ pairwise distinct. 
  Let $\alpha \in \overline{\F}^\times$ be a root of $f$.
  Note that 
  \[
    \alpha f'\left( \alpha \right) = 
    n a_n \alpha^n + m a_m \alpha^m + k a_k \alpha^k =
    (m - n) a_m \alpha^m + (k - n) a_k \alpha^k
  \] 
  Therefore, 
\end{document}